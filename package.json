{
  "name": "arena-model-archive",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Private Apple-Silicon archive for Arena model interactions: passive CDP capture, encrypted storage, MCP operation.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --dry 2>/dev/null; tsc -b",
    "test": "vitest run",
    "test:watch": "vitest",
    "doctor": "npx tsx scripts/doctor.ts",
    "gate:p0": "npm run build && npx tsx scripts/gate-p0.ts",
    "clean": "find apps packages -name dist -type d -prune -exec rm -rf {} + ; find apps packages -name '*.tsbuildinfo' -delete"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5",
    "zod": "^3.23.8"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
